[project]
name = "integra-contabil"
version = "0.1.0"
description = "Integração com Integra Contador (parcelamentos PARCSN, PERTSN, RELPSN etc.)"
requires-python = ">=3.10"

[project.dependencies]
fastapi = "*"
uvicorn = "*"
requests = "*"
python-dotenv = "*"
pydantic = "*"
